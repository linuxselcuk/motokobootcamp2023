// In your file called utils.mo: create a function called second_maximum 
// that takes an array [Int] of integers and returns the second largest number in the array.

// second_maximum(array : [Int]) ->  Int;


actor {
    public func second_maximum(array : Int) : async Int{
        
    }
}